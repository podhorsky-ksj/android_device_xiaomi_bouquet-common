hal_client_domain(system_app, hal_mlipay)

# XiaomiParts applies per-application thermal profiles through the legacy
# thermal-message sysfs control node.
allow system_app sysfs_thermal:file rw_file_perms;
